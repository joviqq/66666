<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd  http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.0.xsd           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

	<!-- 安全管理器 -->
	<bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
		<property name="realm" ref="casRealm" />
		<property name="subjectFactory" ref="casSubjectFactory" />
		<!-- <property name="cacheManager" ref="shiroEhcacheManager" /> -->
	</bean>

	<!-- 用来接受ticket的filter -->
	<bean id="casFilter" class="org.apache.shiro.cas.CasFilter">
		<property name="failureUrl"
			value="http://localhost:8080/freepaayManage/resources/failure.html" />
	</bean>
	
	<!-- 登出shiro后，需要重定向到cas去也登出 -->
	<bean id="logoutFilter" class="org.apache.shiro.web.filter.authc.LogoutFilter">
		<property name="redirectUrl"
			value="http://192.168.99.226:8080/cas/logout?service=http://localhost:8080/freepaayManage/manage/myList" />
	</bean>

	<bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
		<property name="securityManager" ref="securityManager" />
		<property name="loginUrl"
			value="http://192.168.99.226:8080/cas/login?service=http://localhost:8080/freepaayManage/cas" />
		<property name="successUrl"
			value="http://localhost:8080/freepaayManage/manage/myList" />
		<property name="filters">
			<map>
				<!-- <entry key="authc" value-ref="formAuthenticationFilter" /> -->
				<entry key="cas" value-ref="casFilter" />
				<entry key="logout" value-ref="logoutFilter" />
			</map>
		</property>
		<!-- 重要：必须加上/cas = cas来告诉/cas用casFilter来处理 -->
		<!-- 重要：logout为登出filter -->
		<property name="filterChainDefinitions">
			<value>
				/manage/**=anon
				/resources/**=anon
				/file/upload/**=anon
				/cas = cas
				/logout = logout
				/** = user
			</value>
		</property>
	</bean>

	<bean id="casRealm" class="org.apache.shiro.cas.CasRealm">
		<property name="defaultRoles" value="ROLE_USER" />
		<!-- 重要：这个是用来获取ticket的casServerURL的前缀，所以不能有login -->
		<property name="casServerUrlPrefix" value="http://192.168.99.226:8080/cas/" />
		<!--客户端的回调地址设置，必须和上面的shiro-cas过滤器拦截的地址一致 -->
		<property name="casService" value="http://localhost:8080/freepaayManage/cas" />
	</bean>

	<bean id="casSubjectFactory" class="org.apache.shiro.cas.CasSubjectFactory" />

	<bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor" />

</beans>